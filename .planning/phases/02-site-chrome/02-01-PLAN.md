---
phase: 02-site-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/AnnouncementBar/AnnouncementBar.tsx
  - src/sections/AnnouncementBar/index.ts
  - src/registry.ts
  - test/sections-chrome.test.tsx
autonomous: true
requirements:
  - CHR-01
must_haves:
  truths:
    - "Merchant can add/remove/reorder social-link child blocks in AnnouncementBar"
    - "AnnouncementBar renders locationLabel, changeLabel, followLabel text props"
    - "AnnouncementBar renders nothing (not a placeholder card) when social-link slot is empty"
    - "yarn build and yarn test:contract pass with _demo removed and announcement-bar registered"
    - "AnnouncementBar handles missing/undefined text props without crashing"
  artifacts:
    - path: "src/sections/AnnouncementBar/AnnouncementBar.tsx"
      provides: "AnnouncementBar section component + announcementBarSettingsSchema"
      exports: ["AnnouncementBar", "announcementBarSettingsSchema", "AnnouncementBarProps"]
    - path: "src/sections/AnnouncementBar/index.ts"
      provides: "Barrel export for AnnouncementBar module"
      contains: "export * from"
    - path: "src/registry.ts"
      provides: "Registry with _demo removed, announcement-bar registered in all four applicable maps"
      contains: "announcement-bar"
    - path: "test/sections-chrome.test.tsx"
      provides: "Render-smoke tests for chrome sections (Wave 0 contract)"
  key_links:
    - from: "src/sections/AnnouncementBar/AnnouncementBar.tsx"
      to: "src/lib/blocks-slot.tsx"
      via: "BlocksSlot wrapping renderBlocks with empty={null}"
      pattern: "BlocksSlot"
    - from: "src/registry.ts"
      to: "src/sections/AnnouncementBar/index.ts"
      via: "import barrel; register in sectionsComponents + sectionSettingsSchemas + sectionBlocksConfig"
      pattern: "announcement-bar"
---

## Phase Goal

**As a** merchant, **I want to** see an editable announcement bar with my social links in the customizer, **so that** I can control the page-top chrome (location label, change text, follow prompt, and social icons) without touching code.

<objective>
Remove the _demo scratch section and replace it atomically with the AnnouncementBar section (CHR-01) so the registry is never empty and yarn test:contract stays green. AnnouncementBar accepts social-link shared blocks, renders three editable text props, and uses BlocksSlot with empty={null} so an empty social row renders nothing.

Purpose: This is the first real chrome section and validates the shared social-link block path end-to-end. The _demo removal must happen in the same commit as the first real section per Pitfall 1 (the contract asserts sectionsComponents is non-empty).

Output: AnnouncementBar component + barrel + render-smoke test scaffold + updated registry (D-01 through D-06 compliance established for this section).
</objective>

<execution_context>
@/Users/diegoalba/Documents/theta-theme-starter/.claude/gsd-core/workflows/execute-plan.md
@/Users/diegoalba/Documents/theta-theme-starter/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-chrome/02-CONTEXT.md
@.planning/phases/02-site-chrome/02-RESEARCH.md
@.planning/phases/02-site-chrome/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnnouncementBar section component + render-smoke test</name>
  <files>
    src/sections/AnnouncementBar/AnnouncementBar.tsx,
    src/sections/AnnouncementBar/index.ts,
    test/sections-chrome.test.tsx
  </files>
  <read_first>
    src/lib/blocks-slot.tsx
    src/lib/empty-state.tsx
    src/components/ui/button.tsx
    src/lib/utils.ts
    src/blocks/SocialLink/SocialLink.tsx
    design/AnnouncementBar.dc.html
    test/registration-contract.test.ts
  </read_first>
  <action>
    Create src/sections/AnnouncementBar/AnnouncementBar.tsx:

    Export AnnouncementBarProps interface with optional props: locationLabel (string), changeLabel (string), followLabel (string), renderBlocks (() => React.ReactNode), sectionId (string), sectionName (string).

    Export AnnouncementBar component. Outer element: div with role="banner". Inner layout: "container mx-auto container-padding-x flex flex-wrap items-center justify-between gap-3 py-2" on a div with "bg-brand-navy text-white" on the outer div.

    Left side: a div with "flex items-center gap-3 font-opensans text-sm". Contains: MapPin icon from lucide-react with className "size-4 text-brand-yellow" and aria-hidden. A span for locationLabel (conditionally rendered when truthy). A span for changeLabel with "inline-flex items-center gap-1" when truthy, containing ChevronDown icon with className "size-4" and aria-hidden — per D-06, this is static/decorative (no switcher behavior).

    Right side: a div with "flex items-center gap-4". Contains: a span with "font-opensans text-sm" for followLabel (when truthy). Then BlocksSlot from "@/lib/blocks-slot" with props: renderBlocks={renderBlocks}, empty={null}, className="flex items-center gap-3" — per STATE.md WR-01, empty={null} means nothing renders when the social row is empty (correct for chrome bars).

    Export announcementBarSettingsSchema: array of three setting objects:
    - id "locationLabel", label "Ubicación", type "text", default "SD | República de Colombia"
    - id "changeLabel", label "Texto de cambio", type "text", default "Cambiar"
    - id "followLabel", label "Texto de seguir", type "text", default "Síguenos en:"

    Create src/sections/AnnouncementBar/index.ts as a barrel: export * from "./AnnouncementBar"

    Create test/sections-chrome.test.tsx:
    Import from vitest: describe, it, expect. Import React from "react". Import renderToString from "react-dom/server". Setup externals shims following the pattern from test/registration-contract.test.ts (jsdom/vitest setup for externals — read the existing test file to replicate the shim pattern). Import AnnouncementBar and announcementBarSettingsSchema from "@/sections/AnnouncementBar".

    Test group "AnnouncementBar": test "renders without crash with empty props" — call renderToString(React.createElement(AnnouncementBar, {})) and expect it to be a string. Test "renders locationLabel text" — pass locationLabel "Test Location" and expect the output string to contain "Test Location". Test "renders nothing in social slot when renderBlocks is undefined" — call with empty props and verify no EmptyState heading text appears (the empty={null} means nothing renders).

    All text renders as JSX text nodes — no dangerouslySetInnerHTML anywhere. Icons use aria-hidden. Import order: react → lucide-react → @/lib/* → @/components/ui/*
  </action>
  <verify>
    <automated>cd /Users/diegoalba/Documents/theta-theme-starter && yarn test sections-chrome 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AnnouncementBar.tsx exports AnnouncementBarProps, AnnouncementBar, announcementBarSettingsSchema
    - announcementBarSettingsSchema has exactly 3 entries with ids "locationLabel", "changeLabel", "followLabel"
    - BlocksSlot is imported from "@/lib/blocks-slot" and called with empty={null}
    - No dangerouslySetInnerHTML in AnnouncementBar.tsx
    - MapPin and ChevronDown are from "lucide-react" and have aria-hidden
    - test/sections-chrome.test.tsx: "renders without crash with empty props" passes
    - test/sections-chrome.test.tsx: "renders locationLabel text" passes
    - Outer wrapper element has role="banner"
  </acceptance_criteria>
  <done>AnnouncementBar component is created with correct props, BlocksSlot wiring, and the render-smoke test passes all assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Registry atomic swap — remove _demo, register announcement-bar</name>
  <files>
    src/registry.ts
  </files>
  <read_first>
    src/registry.ts
    src/sections/AnnouncementBar/index.ts
    src/blocks/SocialLink/index.ts
    src/blocks/StoreBadge/index.ts
    test/registration-contract.test.ts
    test/contract-assertions.ts
  </read_first>
  <action>
    Edit src/registry.ts to perform the atomic _demo → announcement-bar swap:

    1. Remove: the import line for Demo and demoSettingsSchema from "./sections/_demo".
    2. Add: import { AnnouncementBar, announcementBarSettingsSchema } from "./sections/AnnouncementBar" below the existing SocialLink and StoreBadge imports.
    3. In sectionsComponents: replace the _demo entry with "announcement-bar": AnnouncementBar cast as React.ComponentType<Record<string, unknown>>.
    4. In sectionSettingsSchemas: replace the _demo entry with "announcement-bar": announcementBarSettingsSchema.
    5. In sectionBlocksConfig: replace the _demo entry with "announcement-bar": { blocks: [{ type: "@theme" }], maxBlocks: 7 }.

    The blocksComponents and blockSettingsSchemas maps for social-link and store-badge are unchanged.

    After editing, sectionsComponents must have exactly one key ("announcement-bar") and sectionBlocksConfig must have exactly one key ("announcement-bar"). This satisfies the contract test's non-empty-sections assertion.

    Important: do NOT delete src/sections/_demo/ yet — that is a separate cleanup; the registry no longer imports it, which is sufficient to remove it from the bundle and pass the contract test.
  </action>
  <verify>
    <automated>cd /Users/diegoalba/Documents/theta-theme-starter && yarn test:contract 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/registry.ts no longer imports from "./sections/_demo"
    - src/registry.ts imports AnnouncementBar and announcementBarSettingsSchema from "./sections/AnnouncementBar"
    - sectionsComponents contains key "announcement-bar" and does NOT contain key "_demo"
    - sectionSettingsSchemas contains key "announcement-bar" and does NOT contain key "_demo"
    - sectionBlocksConfig["announcement-bar"] = { blocks: [{ type: "@theme" }], maxBlocks: 7 }
    - yarn test:contract exits 0 (green) — the contract test passes with the new registry shape
    - yarn build exits 0 (green)
  </acceptance_criteria>
  <done>_demo is removed from the registry, announcement-bar is registered in all four applicable maps, and yarn test:contract + yarn build pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merchant text props → JSX render | locationLabel, changeLabel, followLabel arrive as platform-validated strings; render into JSX text nodes |
| Social-link block output → section DOM | renderBlocks() output is React-managed; no raw HTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AnnouncementBar text props (locationLabel, changeLabel, followLabel) | mitigate | Render all text via JSX text interpolation ({locationLabel}) — React auto-escapes; no dangerouslySetInnerHTML; no richtext/html setting types used |
| T-02-02 | Tampering | ChevronDown static link in changeLabel row | accept | changeLabel rendered as static span/text, no href; no URL injection surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs this plan (RESEARCH.md Package Legitimacy Audit: no new dependencies) |
</threat_model>

<verification>
1. yarn test sections-chrome passes (render-smoke: crash-free with empty props, locationLabel renders)
2. yarn test:contract passes (announcement-bar registered, _demo absent, non-empty-sections assertion green)
3. yarn build passes (TypeScript compiles, IIFE bundle produced)
4. src/registry.ts does not contain "_demo" as a string key in any map
5. AnnouncementBar.tsx does not contain "dangerouslySetInnerHTML"
6. AnnouncementBar.tsx does not contain any hex color literals (uses brand-navy, brand-yellow, text-white tokens only)
</verification>

<success_criteria>
- AnnouncementBar section renders responsively on a navy background with location label, change label (with caret), and follow label text props, and the social-link child block slot is wired via BlocksSlot with empty={null}
- _demo is fully removed from the registry and src/registry.ts has no reference to it
- yarn build and yarn test:contract pass green
- render-smoke test in test/sections-chrome.test.tsx passes all assertions
</success_criteria>

<output>
Create .planning/phases/02-site-chrome/02-01-SUMMARY.md when done.

## Artifacts this phase produces

New files:
- src/sections/AnnouncementBar/AnnouncementBar.tsx — exports AnnouncementBarProps, AnnouncementBar, announcementBarSettingsSchema
- src/sections/AnnouncementBar/index.ts — barrel: export * from "./AnnouncementBar"
- test/sections-chrome.test.tsx — render-smoke tests for chrome sections

Modified files:
- src/registry.ts — removes _demo import + all three _demo map entries; adds announcement-bar in sectionsComponents, sectionSettingsSchemas, sectionBlocksConfig

Registry keys added: "announcement-bar" (sectionsComponents, sectionSettingsSchemas, sectionBlocksConfig)
Registry keys removed: "_demo" (sectionsComponents, sectionSettingsSchemas, sectionBlocksConfig)
Settings schema IDs: locationLabel, changeLabel, followLabel
</output>
